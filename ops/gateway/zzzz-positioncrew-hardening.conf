[Service]
# Install for each gateway unit. This filename sorts after the target host's
# service.d/zzz-lxc-service.conf; never edit that global container drop-in.
NoNewPrivileges=yes
ProtectKernelTunables=yes
ProtectControlGroups=yes
